_app.js: 공통처리
 - Component로 명시된 인자값 부분이 다른파일이 되는거다.
 반응형을 하면 먼저 모바일을 기준으로 해라
gutter: 컬럼사이의 간격
target="_blank": 새탭에서 열기
rel="noreferrer noopener": 보안강화
class쓸당시에는 프리센터,컨테이너를 ui,data로 분리하는걸 추천했으나
hooks를 쓰면서 추천하지않는다.
<div style={{ marginTop: 10 }}>인라인스타일을(를) 쓰면안되는이유
  1. 객체이기 때문에 리액트에서 새로운 객체로 인식
  2. 새로운객체는 리렌더링이 된다.
  3. 따라서 변하지 않았어도 리렌더링이 일어난다.
  4. 단 성능에 크게 영향없다면 상황고려해서 써도가능
  
  useCallBack: 함수를콜백
  useMemo: 값을콜백

리렌더링이 됬다고 전부 다 다시 그리는게 아니라, 바뀐부분만 그린다.
echart

컨테이너를 거쳐서 받아오면 괜히 프롭스만 늘어나서 복잡